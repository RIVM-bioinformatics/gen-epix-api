[pytest]
markers =
    scenario_ids(id): Custom marker for scenario IDs used in test reporting.
# Exclude mutmut's generated workspace to avoid duplicate test collection/import path mismatches.
norecursedirs = mutants
filterwarnings =
    ignore:.*shadows an attribute in parent.*
